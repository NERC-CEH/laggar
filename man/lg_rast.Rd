% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{lg_rast}
\alias{lg_rast}
\title{Summarize raster within incremental buffer zones around points}
\usage{
lg_rast(
  x,
  y,
  func = NULL,
  dist_seq = NULL,
  mindist = NULL,
  maxdist = NULL,
  incdist = NULL,
  plot_doughnuts = TRUE,
  object_n = 1,
  bg_layer = NULL
)
}
\arguments{
\item{x}{An object representing measurement points. Can be an \code{sf} object or
convertible to \code{sf} via internal helper functions.}

\item{y}{A \code{SpatRaster} object from the \code{terra} package representing the raster
to summarize.}

\item{func}{Optional. A function or character string specifying how to summarize
raster values within each buffer. If \code{NULL}, raw cell values are returned.
Accepts functions supported by \code{exactextractr::exact_extract} or a custom
function that takes a numeric vector and returns a single value.}

\item{dist_seq}{Optional numeric vector of buffer distances. If provided, overrides
\code{mindist}, \code{maxdist}, and \code{incdist}.}

\item{mindist}{The minimum distance, ignored if dist_seq is specified}

\item{maxdist}{The maximum distance, ignored if dist_seq is specified}

\item{incdist}{The distance increment, ignored if dist_seq is specified}

\item{plot_doughnuts}{Logical. If \code{TRUE}, plots the doughnut geometries for
visual verification. Highly recommended for error checking.}

\item{object_n}{The measurement point that you want to plot separately. A single or vector of integers.}

\item{bg_layer}{Background layer to plot along with the objects. Currently only accepts \code{terra::spatRaster}.}
}
\value{
A list with three elements:
\describe{
\item{buffer_values}{Matrix or vector of raster values (raw or summarized) for each buffer.}
\item{buffer_area}{Matrix or vector of buffer areas corresponding to each value.}
\item{value_parea}{Numeric vector of values normalized by buffer area.}
}
}
\description{
This function calculates raster values within concentric buffer zones ("doughnuts")
around a set of measurement points. It optionally applies a summary function to
raster values within each buffer and returns raw values or aggregated summaries.
}
\details{
Internally, this function:
\enumerate{
\item Converts \code{x} to an \code{sf} object.
\item Validates that \code{y} is a \code{SpatRaster}.
\item Generates buffer distances using \code{.index_check()}.
\item Builds doughnut geometries and extracts raster values using
\code{values_within_dist_rast()}.
\item Computes value per unit area for each buffer.
}
}
\examples{
\dontrun{
# Example: Summarize mean raster values within 3 buffers around points
lg_rast(x = points_sf, y = raster_layer, func = "mean",
        mindist = 100, maxdist = 300, incdist = 100)
}
}
\seealso{
\code{\link{values_within_dist_rast}}, \code{\link{doughnut_builder}}, \code{\link{doughnut_checker}}
}
